# PUBLIC_INTERFACE
# Example environment variables for The Daily Grind Frontend
# Configure the base URL for the backend API. Leave empty for same-origin (/api/...).
# In production, set to your backend origin, e.g. https://api.thedailygrind.com
REACT_APP_API_BASE=
